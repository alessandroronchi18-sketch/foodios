audit=true
fund=false
save-exact=false
# Audit 2026-06-24: jest-axe@10 + react-dom peer-deps generano ERESOLVE su
# npm install. Vercel build CLI non passa --legacy-peer-deps e va in fail.
# Forziamo a livello progetto cosi' sia vercel CLI che npm install funzionano.
legacy-peer-deps=true
